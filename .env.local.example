# Example env file for local self-hosted mode.
# Copy to .env.local and adjust before running docker compose.

# --- Postgres ---
POSTGRES_USER=app
POSTGRES_PASSWORD=change-me
POSTGRES_DB=artikelregistrering

# --- App ---
# Host port the combined frontend+API will be exposed on.
APP_PORT=3001
